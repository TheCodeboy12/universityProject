#include <stdio.h>
/**
 * @brief Hash function for hash table
 * @return size_t - hash value
 * @param str - string to hash
 * @param arrSize - size of hash table
*/
size_t hash(char *str , size_t arrSize);
